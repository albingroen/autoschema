export enum AppError {
  UNAUTHORIZED = "You don't have permission to do this",
  CREATE_SCHEMA = "Failed to create schema",
  DELETE_WORKSPACE = "Failed to delete workspace",
  CREATE_WORKSPACE = "Failed to create workspace",
}
